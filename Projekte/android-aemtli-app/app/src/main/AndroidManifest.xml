<?xml version="1.0" encoding="utf-8"?>
<manifest package="ch.nyp.aemtli_app"
		  xmlns:android="http://schemas.android.com/apk/res/android"
	>

	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme"
		>
		<activity
			android:name=".gui.activity.LoginActivity"
			android:label="@string/app_name"
			>
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity
			android:name=".gui.activity.ApprenticesActivity"
			android:label="@string/allelernendenactivity_activityname"
			/>
		<activity android:name=".gui.activity.MyDutiesActivity">
		</activity>
		<activity android:name=".gui.activity.AllDutiesActivity">
		</activity>
		<activity android:name=".gui.activity.ProfileActivity">
		</activity>
	</application>

</manifest>